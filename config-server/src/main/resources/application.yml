server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/duarte900/teste.git
          ignore-local-ssh-settings: true
          default-label: teste
          searchPaths: Properties*